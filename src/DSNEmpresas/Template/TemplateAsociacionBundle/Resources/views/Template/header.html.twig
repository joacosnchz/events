{% block header %}
<header class="wrapper-header">
    {% if app.user %}
        <table class="tableHeader" style="background:url({{ asset('bundles/templateasociacion/images/agencias/') }}{{ app.user.idagencia.path }}) no-repeat;">
    {% else %}
        <table class="tableHeader" style="background:url({{ asset('bundles/templateasociacion/images/agencias/jornadas_top.fw.png') }}) no-repeat;">
    {% endif %}
            <tr>
                <td>
                    <h2 align="center">{% block headertitle %}{% endblock %}</h2>
                </td>
            </tr>
        </table>
    </header>
{% endblock %}
